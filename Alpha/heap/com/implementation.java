package heap.com;
// heap can be of 2 types --> min heap and max heap
//min --> parent <= child
// max --> parent >= child

// heap can be of 2 types --> min heap and max heap
// min --> parent <= child (root is smallest)
public class implementation {
    public static void main(String[] args) {
        // implementing the heap data structure
        // by default heap is a min heap in which the parent <= child

    }
}
