import java.util.Arrays;

public class RandomKnowledgeOnArray {
public  static void main(String[]args){
    Integer[]wrapperArr = new Integer[5]; // this is an object and intially all the object have
    // null value in them.
    int[]arr= new int[5];

    System.out.println(Arrays.toString(wrapperArr));
    System.out.println(Arrays.toString(arr));
    }
}
