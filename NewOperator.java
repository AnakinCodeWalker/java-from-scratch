public class NewOperator {
    // use to create the dynamic memory allocation
    // used to create objects during the runtime.
    public static void main(String[]args){

    }

}

class Array{

int []arr = {1 , 3, 4} ; // internally both  are doing the same thing.

int []array = new int[5];
// whatever is written before the = will happen at compile time 

// whatever is written after the = will happen at run - time 
// object in the heap memory is actually being created in the heap memory during the run time.
// by the help of the new keyword.

String[] str = new String[10];
// will create an array of string inside the meomory.

}
